// try just manipulating the DOM
// no JSX, no reactive, pure DOM-DOM!

/*
A node comes in as:
   {
        [type: control]
        [id: string]
        [props: {}]
        [children: [...] |
        text: string]
   }

the dom is manipulated, that's right, and not just insert/deleted
*/
const _root =  document.getElementById("app");
const _dom = {}
export const appendChild = (parent, child) => {
    
} 

export const render = (controls, parent = "app") =>
{

}